# Lab 9 - JavaScript Debugging and Error Handling

**Name:** Ori Chason  
**Partner:** Solo submission (no partner)  
**GitHub Pages:** _link TBD_

Demonstrates the browser Console API (log, error, warn, count, assert, clear, dir, dirxml, group, table, time, trace), try/catch/finally with a custom `CalculatorError` subclass, uncaught global error handling via `window.addEventListener('error', ...)`, and error monitoring with TrackJS.
